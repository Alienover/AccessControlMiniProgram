const USER_KEY = 'user'
const MACHINE_KEY = 'machines'

const API_VERSION = "7.0.0"

const UNLOCK_TYPE = 1

const LOGIN_TYPE = "1"

const CODE = ""

const SECRET = 'secret11'

const FRONT_MARK = '自动门'

module.exports = {
  USER_KEY,
  MACHINE_KEY,
  API_VERSION,
  UNLOCK_TYPE,
  SECRET,
  LOGIN_TYPE,
  CODE,
  FRONT_MARK
}